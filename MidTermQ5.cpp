#include <iostream>
#include <cmath>

using namespace std;
/*
int main() {
    int firstNum;
    int secondNum;
    int i;
    int sum = 0;
    int square;
    int square_sum = 0;
    char letter = 'A';


    cout << "Enter two integers from lesser to higher." << endl;;
    cin >> firstNum >> secondNum;

    if (firstNum > secondNum)
    {
        cout << "the first Number must be lower than second number." << endl;
        return 0;
    }

    while (firstNum < secondNum)
    {
        cout << "Printing Odd Values between " << firstNum << " & " << secondNum << endl;
        for (i = firstNum + 1; i < secondNum; i++)
        {
            if (i % 2 != 0)
            {
                cout << i << endl;
            }
        }
        cout << "Printing Sum of even values between " << firstNum << " & " << secondNum << endl;
        for (i = firstNum + 1; i < secondNum; i++)
        {
            if (i % 2 == 0)
            {
                sum = sum + i;
            }
        }
        cout << sum << endl;
        break;
    }
    cout << "Printing numbers 1-10 and their square values." << endl;
    i = 1;
    while (i <= 10) {
        cout << i << " " << pow(i, 2) << endl;
        i++;
    }
    i = firstNum + 1;
    while (i < secondNum)
    {
        if (i % 2 != 0)
        {
            square = pow(i, 2);
            square_sum += square;
        }
        i++;
    }
    cout << "Printing the alphabet in all uppsercase." << endl;
    while (letter <= 'Z')
    {
        cout << static_cast<char>(letter) << endl;
        letter = static_cast<char>(letter) + 1;
    }
}
*/