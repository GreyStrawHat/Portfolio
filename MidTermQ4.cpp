#include <iostream>
/*  
using namespace std;

int main() {

    int integer;
    int even_sum = 0;
    int odd_sum = 0;

    cout << "Enter a set of integers to get the sum of the even and odd values." << endl;
    cout << "Hit Ctrl + Z then Enter once finish for the result." << endl;

    cin >> integer;

    while (cin)
    {
        if (integer % 2 == 0)
        {
            even_sum = even_sum + integer;
            cin >> integer;
        }
        else
        {
            odd_sum = odd_sum + integer;
            cin >> integer;
        }

    }

    if (even_sum > 0)
    {
        cout << "The sum of the even values is " << even_sum << "." << endl;
    }
    if (odd_sum > 0)
    {
        cout << "The sum of the odd values is " << odd_sum << "." << endl;
    }

    cout << "Now Exiting The Program..";
    return 0;
}

*/